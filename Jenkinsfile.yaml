pipeline:
  agent:
    any:
  stages:
    - stage: "Log Jenkins Maven Docker Java and Git versions"
      steps:
        - sh "mvn -version"
        - sh "java -version"
        - sh "git --version"
    - stage: "Jenkins YAML Maven Build"
      steps:
        ##- git "https://github.com/sgottapu37/Jenkinstest.git" 
        - sh "ls -ltr"
        - sh "cd my-app"
        - sh "mvn compile"
    - stage: "Final Jenkins YAML Pipeline Stage"
      steps:
        - sh "echo 'Jenkins YAML Pipeline Complete'"
