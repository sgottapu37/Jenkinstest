pipeline:
  agent:
    any:
  stages:
    - stage: "Build"
      steps:
        - echo "Build is Successful"
    - stage: "Code-Scan"
      steps:
        - echo "Code scan is successful"
    - stage: "Deployment"
      steps:
        - echo "Deployment is successful"
